// The internationalization middleware has been disabled to fix a build error.
// The file is kept for potential future use.

export function middleware() {
  // This middleware is disabled and does nothing.
}

export const config = {
  // This matcher is intentionally empty, so the middleware will not run on any pages.
  matcher: [],
};
